#!/bin/sh

scriptpath=$(readlink -f "${0}")
basedir=$(dirname "${scriptpath}")
builddir=${basedir}/builddir


edo() {
    [ -z ${DEBUG_SCRIPTS} ] || echo "${@}"
    "${@}" || exit 100
}

_check() {
    edo make -j -C "${builddir}" check "${@}"
}

_main() {
    _check "${@}"
}

_main "${@}"
